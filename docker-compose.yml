version: '3.8'
services:
  backend:
    image: bingebook-backend
    ports:
      - "3002:3001"
    env_file:
      - ./apps/backend/.env

  frontend:
    image: bingebook-web
    ports:
      - "3000:3000"
    depends_on:
      - backend